cd
mkdir
file
cat
touch
nano
vim
pwd
cp
rm
mv
ls 
file ( позволяет узнать тип данных, которые на самом деле содержатся внутри документа)
grep (поиск фаилов, текстов с нужной строчкой, отфильтрует из вывода нужные строки (по шаблону))
xargs (ывод предыдущей команды можно передать в аргументы следующей)
head (выывод -n строк с начала)
tail (выывод -n строк с конца)
less (вывод содержимого длинного файла)
find (найти)
date (дата)
echo (вывести текст)
explore (открыть на папку на к)
curl (отправить запрос)
--help
git add
git commit
git push
git pull
git branch
git clone
git merge
git fetch
history
    +спец. символы
    ~ домашний каталог
    . текущая дерриктория
    .. родительский каталог, относительно текущей
    / разделитель
    # комментарий или обрезка строк
    ? как знак подстановки символов
    * как "подстановочный" знак последовательности символов
    [] набор символов
    ; разделитель команд оболочки
    & "фоновый процесс"
    < перенаправление ввода
    > Перенаправление вывода
    | объединяет команды
    ! логический оператор НЕ и оператор истории
    $ выражения переменных
    
